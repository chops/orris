#!/usr/bin/env bash
# Standalone ExUnit entrypoint; also included in the console's ordinary mix test suite.
set -euo pipefail
here=$(cd "$(dirname "$0")/.." && pwd)
exec elixir -e 'ExUnit.start(); [path] = System.argv(); Code.require_file(path)' "$here/test/tooling/gate_controls_test.exs"
