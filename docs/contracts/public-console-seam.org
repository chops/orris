#+title: Public console seam contract (Prepare, Query, top-level exports)
#+date: 2026-09-09
#+status: GREEN rev4 (successors of 0ebffdf: physical scope traversal for targets and configured roots, confined host counts, cancel_reason drift removed, opaque Prepared readers); RED rev3 was ec5a20a at 2c33d78 (TEST/DOC only); map: logs/liveview-console-map/claude/B3-PUBLIC-CONSOLE-API-MAP.org rev3 (A1-A4 folded)

* Obligation
An external application (the console) reaches the core ONLY through modules the top boundary exports:
=AiOrchestrator.Commands=, =AiOrchestrator.Prepare= and =AiOrchestrator.Query=. Reads are projection-derived; the
verified journal prefix is used and =pending_repair= is exposed as data, never executed; nothing is written by a read.
Mutations run through =Prepare= (the CLI's own admission rules, shared) and =Commands.invoke=; the public entry uses the
core's built-in foreground executor (=Run.Executor=) chosen inside core (A1). Browser input never selects identity, roots,
paths, executor, adapter, clock or id seams: the public request is exactly a verb and a run handle (=run_ref=).
The CLI keeps its directory-based path, its error maps, exit codes, projection files and its pane-claim lifetime
(the outcome callback runs inside the claimed operation before release, A2) byte for byte.

* Public API (exact shapes; A3)
- =Prepare.validate(run_ref, server_opts)= :: ={:ok, %{spec_hash, plan_hash}} | {:error, rejection}=
- =Prepare.start/2=, =Prepare.resume/2=, =Prepare.cancel/2= :: ={:ok, %Prepare.Prepared{}} | {:error, rejection}= (opaque struct)
- =Prepare.invoke(actor, prepared, server_opts)= :: ={:ok, %{events: [map()], close: :ok | {:error, map()}}} | {:error, rejection}=
- =Prepare.supported_verbs()= == =["start", "resume", "cancel"]=; =Prepare.verb(term)= :: ={:ok, verb} | {:error, %{clause: "command_verb_unsupported"}}=
- =Query.list_runs(server_opts)= :: ={:ok, %{runs: [%{run_ref, run_id, status, last_seq, error}], skipped_outside_root: n}} | {:error, rejection}=
- =Query.run_summary(run_ref, server_opts)= :: ={:ok, %{run_ref, run_id, status, last_seq, summary, rendered, pending_repair}} | {:error, rejection}=
- =Query.run_context(run_ref, server_opts)= :: ={:ok, %{run_ref, rendered, pending_repair}} | {:error, rejection}=
- =Query.host_view(run_ref, server_opts)= :: ={:ok, host_view} | {:error, rejection}= where host_view =
  =%{run_ref, run_id, registered, live, generation, other_registered_directories, mounted_phase, errors}= (A4: the count is
  root-filtered =Host.lookup_run_id= entries excluding this directory; unavailable lookup -> =:unknown=; no pids/paths/handles)
- =Query.resolve(run_ref, server_opts)= :: ={:ok, run_dir} | {:error, %{clause: run_ref_invalid | run_ref_outside_root | runs_root_missing | run_directory_missing | run_directory_invalid}}=
- rejection :: =%{clause: String.t(), detail: map() | nil}=; closed clause set: the resolver clauses above, run_inputs_missing,
  run_inputs_invalid, journal_missing, journal_empty, journal_invalid, pane_claim_refused, pane_release_failed,
  runs_root_unreadable (the root exists but cannot be listed), invalid_prepared (=invoke/3= given something that is not
  a =Prepared=), and every existing command/journal clause passed through; =detail= carries the trusted tier's original
  reason map unchanged. =Prepare.Prepared.t()= is an =@opaque= type: consumers never build or expand it.
- Physical confinement (GREEN rev2, review R1/R2): the resolver walks components in filesystem order, resolving each
  symlink before applying a later "..", so =root/run -> bridge/../actual= with =bridge= pointing outside is refused;
  root "/" is an accepted configured root; the host count applies the same confinement to registered directories.
  The configured root is walked the same way (GREEN rev3, review R1 follow-through): made absolute without lexical
  collapse, every component must exist on the operating system (only a directory may precede further components),
  and containment is strict (the root is never inside itself). This is stationary-filesystem correctness; the concurrent-rename race stays outside the design as stated.
- server_opts (server-only, never from a request): =root=, =budget_ms= (default 1_000; bounds the host observation legs
  after resolution and the verified read, A4), plus the CLI's seam keys (fs, id, clock, dispatch, gate, pane registry, operator).
- Private tier =Prepare.Trusted= (not exported by the top): directory-based =validate/1=, =start/2=, =resume/2=, =cancel/2=,
  =invoke(actor, prepared, executor, outcome)= where =outcome= runs while the claim is held (A2).

* Rows (literal inventory: exact test names; RED rev2 successor of 71c2ff2 for review R1-R5)
Controls, =test/contracts/public_console_seam_controls_test.exs= (C-0..C-9 are the historical baseline: they hold at
2c33d78 and must keep holding; C-10 and later are GREEN regressions adopted from review probes and need the new
APIs, so they do NOT hold at 2c33d78):
- "C-0 the consumer harness is valid: a public-only reference compiles under WAE"
- "C-1 a private reference to AiOrchestrator.Journal.Writer is rejected under WAE with the forbidden-reference diagnostic"
- "C-2 a private reference to AiOrchestrator.Run.Executor is rejected under WAE with the forbidden-reference diagnostic"
- "C-3 a private reference to AiOrchestrator.Host is rejected under WAE with the forbidden-reference diagnostic"
- "C-4 the console actor class is admitted for the three verbs; an agent actor is refused"
- "C-5 Reader: a torn tail loads with a truncate plan and unchanged bytes; a hard-invalid journal fails closed"
  (pins =pending_repair.action == :truncate_tail=, =truncate_bytes == 18=)
- "C-6 CLI preservation: validate, status, cancel and list with absolute and relative directories"
  (validate abs+rel, status --json abs+rel == expected.json, list --json via =cwd:= project root, cancel rel, status 66)
- "C-7 claim lifetime: the outcome precedes release; a release failure keeps the legacy exit 70 with projections written"
  (ObservingRegistry reports whether run-summary.org exists at release; release observed exactly once; A2)
- "C-8 the labelled FAITHFUL witness passes every counting step" (DisposableFaithful: root-filtered, own directory excluded)
- "C-8a the counting rows reject AiOrchestrator.Test.ConsoleSeamDoubles.DisposableFixedZero at step :own_present"
- "C-8b the counting rows reject AiOrchestrator.Test.ConsoleSeamDoubles.DisposableSubtractOnly at step :own_absent"
  (filters roots correctly, then subtracts one unconditionally: passes own-present, fails own-absent)
- "C-8c the counting rows reject AiOrchestrator.Test.ConsoleSeamDoubles.DisposableUnfiltered at step :own_present"
  (the shared counting rows in =test/support/console_seam_rows.ex= name their steps :own_present / :own_absent /
  :unavailable / :privacy; both peer directories exist; witnesses derive the run id from the journal; Monitors are
  started with start_supervised! and the dead one is reaped before use; A4)
- "C-9 a per-leg budget reset witness fails the single-budget elapsed bound" (DisposablePerLegReset takes >= 600 ms for
  budget 300 against a suspended Monitor; suspend/resume in try/after)
- "C-10 scope: a symlink followed by a parent component is traversed in filesystem order" (reviewer probe, R1)
- "C-11 scope: the filesystem root is an accepted configured root" (reviewer probe, R1)
- "C-12 count: a registered symlink escaping the physical root is excluded" (reviewer probe with a real Monitor, R2)
- "C-13 a compound link escape is neither read nor discovered" (summary refused) and "C-13b the listing skips and counts
  both escaping links independently of the summary row" (the listing assertion in its OWN row)
- "C-14 the CLI extraction preserves the formerly ignored cancel_reason option" (reviewer probe, R3)
- "C-15 root: a configured root applies a parent component after physical symlink traversal", "C-16 root: a
  nonexistent component before a parent component is refused as runs_root_missing" and "C-17 scope: strict
  containment excludes the root itself" (reviewer probes on 6947c43, R1 follow-through)
- "C-18 root: a regular file before a parent component is refused as runs_root_missing" (reviewer probe on e0c645d:
  only a directory may precede further components)
Feature rows, =test/contracts/public_console_seam_red_test.exs= (RED at 2c33d78; every row fails on the undefined
=AiOrchestrator.Prepare= / =AiOrchestrator.Query=; F-1/F-1b through the external consumer's compile):
- "F-1 the public seam compiles for a consumer that references only exported modules"
- "F-1b an EXTERNAL consumer process executes a real cancel with public exports only"
  (=mix run= inside the disposable consumer starts its own core dependency application, calls only ConsoleConsumer.Probe
  functions, prints one JSON line; the test asserts exit 0, run_cancelled among the kinds and events.head; A1/R1)
- "F-2 cancel parity with the CLI writer probe" (event kinds, Fold.summary and events.head equal the CLI cancel)
- "F-3 start parity with the CLI on equivalent fixtures, input bytes hashed independently"
  (FixedId/FixedClock reset before each side, the CLI test's doubles as server seams; Prepared.args equal the sha256 of the
  bytes computed by the test; first event run_created carries that spec_hash; event kinds equal the CLI run; R2)
- "F-3b resume parity, input drift and the explicit empty-journal restart" (provenance re-anchored fixture; verb "resume";
  kinds equal the CLI resume; drifted plan bytes -> the CLI's provenance reason in =detail=; empty journal -> verb "start",
  =restart_empty: true=)
- "F-3c a refused pane claim is reported before the Writer decides, as the CLI reports it" (RefusingRegistry; no events.jsonl)
- "F-3d real input failures map to the CLI reasons" (missing spec.json -> run_inputs_missing/file_not_found; chmod 000 plan.json -> run_inputs_invalid/file_read_failed)
- "F-5 the resolver is closed" (run_ref_invalid x9, runs_root_missing, run_directory_missing, run_directory_invalid,
  run_ref_outside_root via symlink, symlinked root accepted; nothing written outside)
- "F-6 discovery applies the scope filter" (runs under root; escaping symlink counted in skipped_outside_root; file skipped)
- "F-7 unknown states under ONE total budget with blocking host legs" (no entry -> registered false; a suspended Monitor
  -> registered and count :unknown, errors for :status and :lookup REQUIRED and a closed :mounted error PERMITTED,
  elapsed < 550 ms for budget 300 (per-leg would be >= 600, proven by C-9); suspend/resume in try/after; run_id still
  from the verified journal; A4/R3)
- "F-8 counting excludes this directory, ignores other roots, reports unknown lookups and leaks no identity"
  (real Monitor records with live pids: own + same-root peer + outside-root canary -> 1; own absent + two peers -> 2;
  dead Monitor -> :unknown with a :lookup error; no #PID/#Reference/canary path in the view; R3)
- "F-9 pending_repair is exposed exactly as the Reader plans it; missing and empty journals map to their clauses"
  (=pending_repair == Reader.load(...).pending_repair= with action :truncate_tail; summary of the verified prefix; bytes
  unchanged; not-json -> journal_invalid; "" -> journal_empty; absent -> journal_missing; R4)
- "F-10 projection files are outputs, never inputs: canaries deleted, summary AND context unchanged, nothing recreated"
  (seeded canary run-summary.org/run-context.org; expected rendered summary/context computed independently from the
  verified fixture; File.rm!; both Query results unchanged; files not recreated; R4)
- "F-13 the supplied console actor is journaled by the built-in path (class and id preserved)"
  (run_cancel_requested.data.requested_by class "console", id "session_abc", verb "cancel"; R3)
- "F-14 a valid agent-class actor is refused for cancel and the journal bytes and head stay unchanged"
  (Policy clause command_not_authorized through the public wrapper; no events.head; R3)
- "F-15 an invalid actor shape is refused before any execution" (invalid_command_actor / command_actor_fields / command_actor_id; bytes unchanged)
- "F-11 unsupported verbs are refused with the existing clause"
- "F-12 the public request is a verb and a handle only"
Preservation (unchanged, must stay green): the CLI test set and the verifier's escript probe; host mount and review controls;
production escript 8/8 with the same 18 applications (no dependency added); Boundary spec in dev and test; Policy table;
=Run.Executor= verbs; contracts suite; Reader tests.
Fixtures (R5): every test tree is created by =ConsoleSeamRows.fresh/1= (exclusive mkdir, random name under the project
build directory, cleanup registered per tree); permission fixtures restore their mode on exit; the consumer harness
removes its project on exit.

* Bounded amendments beyond extraction (explicit)
Top export list; boundaries =Prepare= (deps + Run for the built-in executor) and =Query= (leaf); =Scope.resolve= and its five
clauses; =supported_verbs/0= / =verb/1=; the CLI rewired onto =Prepare.Trusted= with the outcome callback inside the claim.
Not in this slice: Fs injection for input reads; a mounted Commands adapter; live mounted cancel; any listener, session
binding, CSRF/Origin controls, assets, Ash.
